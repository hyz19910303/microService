spring:
  cloud:
    config:
      name: microservice-config-eureka-client-9003   #需要从github上读取的资源名称，注意没有yml后缀名
      profile: dev
      label: master
      uri: http://localhost:3333     #SpringCloudConfig获取的服务地址 即config-server地址